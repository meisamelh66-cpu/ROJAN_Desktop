namespace Rojan.Server.Infrastructure;

/// <summary>
/// Sprint 8 Commit 1: Backend Foundation. This marker exists purely so
/// other layers/tooling have a stable type to anchor
/// <c>typeof(AssemblyMarker).Assembly</c> reflection against (e.g. a
/// future assembly-scanning DI registration) - same role
/// <see cref="Application.AssemblyMarker"/> plays for its own layer.
/// </summary>
public static class AssemblyMarker
{
}
