{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "_comment": "Local-machine default only, not a secret - points at the docker-compose Postgres service (see Rojan.Server/docker-compose.yml). Override with User Secrets (`dotnet user-secrets set ConnectionStrings:DefaultConnection ...`) or the ConnectionStrings__DefaultConnection environment variable for anything real.",
    "DefaultConnection": "Host=localhost;Port=5432;Database=rojan_dev;Username=postgres;Password=postgres"
  }
}
