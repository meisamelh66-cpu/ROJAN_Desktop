# ROJAN AI — TEAM 3 — PHASE 8.144 — INSTALLER COMPLETION REPORT v1

**Type:** Installer build + validation. **No `.cs` / `.xaml` / project / branch change. No commit.** Only: build-tool install + installer generation + install/uninstall validation.
**Branch:** `feature/team3-desktop-completion` @ `da0c36b` (unchanged, tracked tree clean, `origin/main` `77414de`) · **Reference:** `ROJAN_PHASE8_143_DESKTOP_LOCAL_INSTALL_VALIDATION_REPORT_v1.md`

## RESULT: ✅ real Windows installer built, installed, launched to the **login screen**, and cleanly uninstalled

The Phase 8.143 gap — "❌ real Windows installer package not generated (Inno Setup unavailable)" — is **closed**.

---

## A. INSTALLER TOOLCHAIN STATUS

| Check | Before | After |
|---|---|---|
| `ISCC.exe` (Inno Setup compiler) | **absent** | **`%LOCALAPPDATA%\Programs\Inno Setup 6\ISCC.exe`** — Inno Setup **6.7.3** |
| Install method | — | `winget install --id JRSoftware.InnoSetup --exact` — per-user, no admin, installer hash verified by winget. **This is the official build dependency named in `build/publish-installer.ps1`'s own doc comment** ("winget install --id JRSoftware.InnoSetup"). |
| `signtool.exe` | absent | still absent — **not needed** (unsigned build; no certificate) |
| `build/installer/RojanReception.iss` | present, unchanged | present, unchanged — read-only input |
| `build/publish-installer.ps1` | present, unchanged | present, unchanged — the driver script |
| Signing hooks | `#ifdef SignInstaller` in `.iss` + `-CertificatePath` params in `publish-installer.ps1` | inert (default unsigned path) — **verified present, not exercised** |

**No project source touched.** Inno Setup is now installed on this machine as a build tool (deliberate, one-time, as the script's doc comment prescribes) and remains available for future packaging runs.

---

## B. INSTALLER ARTIFACT PATH

Built via `pwsh build/publish-installer.ps1` (unmodified) → re-ran `publish.ps1` (fresh self-contained single-file `win-x64` Release publish) → `ISCC.exe /DAppVersion=1.0.0 RojanReception.iss` → **"Successful compile (70.578 sec)"**.

| Item | Value |
|---|---|
| **Installer** | **`artifacts\ROJAN Reception Setup.exe`** |
| Checksum sidecar | `artifacts\ROJAN Reception Setup.exe.sha256` (written — CI parity) |
| Companion ZIP | `artifacts\RojanDesktop-v1.0.0-win-x64.zip` (73,887,804 bytes) — regenerated by the same run |
| Both under | `artifacts/` — **git-ignored** (build output; tracked tree stays clean) |

**Preserved (verified on the built installer + the payload exe):**

| Property | Value | Preserved from `.iss` / `Directory.Build.props` |
|---|---|---|
| `AppId` | `{D804D0AC-BF41-4A54-8904-D9EC1BB773CF}` | ✅ fixed GUID, unchanged |
| Installer `ProductName` | `ROJAN Reception` | ✅ |
| Installer `ProductVersion` | `1.0.0` | ✅ (`/DAppVersion` from `get-version.ps1`) |
| Payload exe `ProductName` / `ProductVersion` / `FileVersion` / `CompanyName` | `ROJAN Reception` / `1.0.0+da0c36b…` / `1.0.0.0` / `ROJAN` | ✅ |
| Embedded icon | `RojanReception.ico` on the installer wizard **and** the payload exe (extracted 32×32, non-null) | ✅ |
| Config | Release, self-contained, single-file, `win-x64` | ✅ |

---

## C. INSTALLER SIZE / HASH

| | |
|---|---|
| **Size** | **54,057,848 bytes (51.55 MB)** |
| **SHA-256** | **`69CB1F29D9D92541DA8C68F926C96FBE3610F811BF95663FF532152713097615`** |
| Signature | **NotSigned** — expected; no code-signing certificate. SmartScreen will warn "Unknown Publisher" on first run on a real user machine until a cert is obtained (`docs/team3/phases/…` / `docs/standards/code-signing.md`). Signing hooks are ready and inert. |
| Compression | LZMA2 solid |
| Compiler | Inno Setup 6.7.3 CLI |

*(For reference, the Sprint-2 checklist recorded a 53,943,864-byte installer at an earlier commit / different icon+version state; the difference is expected — different payload, different commit hash in the informational version.)*

---

## D. INSTALL VALIDATION

**Clean install** — pre-removed `%LOCALAPPDATA%\Programs\ROJAN Reception` and `%LOCALAPPDATA%\RojanDesktop`; confirmed no Add/Remove Programs key; then `ROJAN Reception Setup.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART /LOG` → **exit code 0**, "Installation process succeeded", "Need to restart Windows? No".

| Check | Result |
|---|---|
| **Install target** | `%LOCALAPPDATA%\Programs\ROJAN Reception\` (per-user, **no admin prompt** — `PrivilegesRequired=lowest`) ✅ |
| Files laid down | `Rojan.Desktop.Shell.exe` (166 MB) + `Languages\{ar-SA,de-DE,en-US,fa-IR}.pack` + `.pdb`/`.xml` + **`unins000.exe`** (4.4 MB) + `unins000.dat` ✅ |
| **Start Menu shortcut** | `…\Start Menu\Programs\ROJAN Reception\ROJAN Reception.lnk` → the exe; icon inherited ✅ |
| Start Menu uninstall shortcut | `…\ROJAN Reception\Uninstall ROJAN Reception.lnk` ✅ |
| **Desktop shortcut** | **absent** — correct: the `desktopicon` task is `Flags: unchecked` and a silent install does not opt in ✅ |
| **Add/Remove Programs entry** | `HKCU\…\Uninstall\{D804D0AC-…}_is1` — `DisplayName: ROJAN Reception`, `DisplayVersion: 1.0.0`, `Publisher: ROJAN`, `DisplayIcon → Rojan.Desktop.Shell.exe`, `UninstallString → unins000.exe`, `InstallLocation` set, `EstimatedSize 176646` KB ✅ |
| **Uninstall entry** | `unins000.exe` present + reachable via ARP and the Start-Menu shortcut ✅ |
| Correct version | ✅ `1.0.0` (ARP) / `1.0.0+da0c36b…` (exe) |
| Correct icon | ✅ embedded, extractable |
| Correct application name | ✅ `ROJAN Reception` everywhere (ARP, shortcut, exe metadata); **window title `ROJAN Reception`** |

### Launch of the installer-installed app (Task D)

Launched `%LOCALAPPDATA%\Programs\ROJAN Reception\Rojan.Desktop.Shell.exe` (bounded run, terminated after observation):

| Check | Result |
|---|---|
| Startup success | ✅ PID 5780, ran 30 s+, `Responding = True`, `WS ≈ 129 MB` |
| Missing DLL / resource errors | ✅ **none** — `stderr` empty |
| **Main window renders** | ✅ **`MainWindowTitle = "ROJAN Reception"`**, **420 × 540** real application window (not a dialog) |
| **Login screen** | ✅ **fully rendered** — "ROJAN Reception" heading, subtitle "ورود به روژان دسکتاپ", **"شماره موبایل" (mobile-number) field** with a Persian-digit input, purple **"ارسال کد" (Send code)** button (screenshot §F) |
| **Localization loads** | ✅ all UI text in Persian (fa-IR), RTL layout, Persian numerals — the `Languages\*.pack` + culture pipeline work |
| **Theme / branding loads** | ✅ purple-accented modern theme, the ROJAN brand mark in the title bar |
| **Logger initializes** | ✅ `%LOCALAPPDATA%\RojanDesktop\logs\` created (no warn/error to flush on this clean run) |
| Clean first-run DB bootstrap | ✅ `stdout` shows `CREATE TABLE "__EFMigrationsHistory"` + `INSERT INTO "__EFMigrationsHistory"` — SQLite DB created fresh and migrations applied; `database/`, `identity/` dirs created |
| **No-backend graceful handling** | ✅ this clean run went **straight to the login screen** (no persisted session → nothing to load → no backend call needed to show login). Phase 8.143's retry MessageBox was triggered by stale `identity/` data from a prior run; a genuinely clean install shows the login form directly with no error. |

**This is a stronger result than Phase 8.143** — the installer-installed, clean-machine first run reaches the **login screen**, not just the init-retry prompt.

---

## E. UNINSTALL VALIDATION

`%LOCALAPPDATA%\Programs\ROJAN Reception\unins000.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART` → **exit code 0**.

| Check | Result |
|---|---|
| Application removed | ✅ `%LOCALAPPDATA%\Programs\ROJAN Reception\` — **gone** |
| Start Menu folder removed | ✅ `…\Start Menu\Programs\ROJAN Reception\` — **gone** (both shortcuts) |
| Desktop shortcut | ✅ n/a (never created) — still absent |
| **Add/Remove Programs entry removed** | ✅ `HKCU\…\Uninstall\{D804D0AC-…}_is1` — **gone** |
| App settings / session removed | ✅ `%LOCALAPPDATA%\RojanDesktop\` — **gone** (per the `.iss` `[UninstallDelete]` — theme/language/API-env JSON + DPAPI session data; a deliberate clean removal) |
| **No broken entries** for `ROJAN Reception` | ✅ zero traces of this phase's install anywhere (dir / Start Menu / Desktop / ARP) |

### Pre-existing observation (NOT this phase's artifact, NOT touched)

The machine carries orphaned folders from an **earlier, different** local-install effort — **`ROJAN Desktop`** (not `ROJAN Reception`):
- `%LOCALAPPDATA%\Programs\ROJAN Desktop\`
- `%LOCALAPPDATA%\Programs\ROJAN Desktop (Local Test - f7d1150)\`
- `%APPDATA%\…\Start Menu\Programs\ROJAN Desktop.lnk`

These have **no Add/Remove Programs entry** (nothing registered), predate Phase 8.144, and belong to a separate prior test (commit `f7d1150`). Left untouched — cleaning arbitrary pre-existing user folders is outside this phase's scope. Flagging so a future clean-VM test starts from a known state.

---

## F. SCREENSHOTS

| File | Content |
|---|---|
| `…/scratchpad/rojan_installed_firstrun.png` (13.8 KB) | **The login screen** of the installer-installed app: title bar "ROJAN Reception" + brand mark; "ROJAN Reception" heading; subtitle "ورود به روژان دسکتاپ" (Login to ROJAN Desktop); "شماره موبایل" (Mobile number) label + input field with Persian-digit content; purple "ارسال کد" (Send code) button. Demonstrates: installer → install → launch → **real window** → full fa-IR RTL localization → themed/branded UI, all from a clean install. |
| `…/scratchpad/rojan_firstrun.png` (Phase 8.143, 6 KB) | prior run's init-retry MessageBox (triggered by stale session data) — kept for contrast. |

---

## G. REMAINING RELEASE BLOCKERS

Installer generation + validation is **DONE**. The remaining blockers are unchanged from `ROJAN_PHASE8_134_DESKTOP_RELEASE_HANDOFF_REPORT_v1.md` and are external to Team 3:

| # | Blocker | Owner | Status after 8.144 |
|---|---|---|---|
| B1 | Installer **unsigned** — SmartScreen "Unknown Publisher" | Release Engineering / procurement | **hooks proven** — the `.iss` `#ifdef SignInstaller` + `publish-installer.ps1 -CertificatePath` path is present and inert; a purchased cert plugs in with zero redesign. Only the certificate is missing. |
| B2 | No **live end-user login test** (real OTP → session → dashboard) | Release Engineering / QA (needs a real phone) | login screen now confirmed to render from a clean install; the OTP round-trip still needs a real phone + reachable backend |
| B3 | **Clean-VM install** not performed on a machine with no .NET runtime | Release Engineering / QA | the installer is now available to run through that test; self-contained bundling already proven |
| B4 | **Release pipeline** (`release.yml`) never run via a real tag | Release Engineering | the `publish-installer.ps1` path it invokes is now end-to-end verified locally |
| B5 | Inventory / HR / Accounting **backend contracts** on `Fake*Repository` | Team 1 | unchanged |
| B6 | POS `ChargeAsync` **payment-idempotency** | Product + Backend | unchanged |
| B7 | **First-launch API-environment default = `Development`** (`localhost:8080`) | Product / DevOps | unchanged; the clean-install login-screen path does not hit it (no backend call to show login), but any *login attempt* would target `localhost:8080` by default — the decision still stands |

**New, minor (not a blocker):** pre-existing orphaned `ROJAN Desktop` folders on this machine (§E) — housekeeping for whoever owns the test environment.

---

## STOP

Phase 8.144 installer completion done. **No `.cs` / `.xaml` / project / branch change; no commit** — `feature/team3-desktop-completion` @ `da0c36b`, tracked tree clean, `origin/main` `77414de`.

**Toolchain:** Inno Setup **6.7.3** installed via `winget` (the build dependency named in `publish-installer.ps1`; per-user, no admin). **Installer built:** `artifacts\ROJAN Reception Setup.exe` — **54,057,848 bytes**, **SHA-256 `69CB1F29D9D92541DA8C68F926C96FBE3610F811BF95663FF532152713097615`**, **unsigned** (no cert; hooks ready), `ProductName ROJAN Reception`, `Version 1.0.0`, `AppId {D804D0AC-…}` preserved, icon embedded.

**Install validation:** silent install → exit 0 → per-user `%LOCALAPPDATA%\Programs\ROJAN Reception\`, Start-Menu shortcut + uninstall shortcut, Add/Remove Programs entry (`ROJAN Reception` / `1.0.0` / `ROJAN`), `unins000.exe`; **no desktop shortcut** (task unchecked, correct). **Launch:** the installer-installed app reached the **login screen** (title "ROJAN Reception", "شماره موبایل" field + "ارسال کد" button), full fa-IR RTL localization + themed/branded UI, clean SQLite DB bootstrap, file logger initialized, **no missing-DLL/resource errors**. **Uninstall validation:** silent uninstall → exit 0 → install dir, Start-Menu folder, ARP key, and `%LocalAppData%\RojanDesktop` all removed; **no broken entries** for this phase's artifact.

**Remaining release blockers unchanged** (B1–B7, all external): the installer is now available to Release Engineering for signing, clean-VM testing, and the first real `release.yml` run.

**Awaiting Phase 8.145 authorization.**
