namespace Rojan.Desktop.Shell;

internal static class Program
{
    [STAThread]
    private static void Main()
    {
        // Composition root and application startup are Phase 05 (Desktop Shell)
        // deliverables. This stub exists only so the executable builds.
    }
}
