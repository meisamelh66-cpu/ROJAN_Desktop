namespace Rojan.Desktop.Application.Calendar;

/// <summary>A specialist who has at least one working-hours entry - the pick-list Presentation uses to choose whose calendar to view.</summary>
public sealed record ScheduledSpecialistDto(string Id, string Name);
