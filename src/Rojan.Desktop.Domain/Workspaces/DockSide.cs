namespace Rojan.Desktop.Domain.Workspaces;

/// <summary>Which edge of the workspace a <see cref="DockedPanelState"/> is pinned to.</summary>
public enum DockSide
{
    Left,
    Right,
    Bottom,
}
