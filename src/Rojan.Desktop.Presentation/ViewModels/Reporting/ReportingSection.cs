namespace Rojan.Desktop.Presentation.ViewModels.Reporting;

/// <summary>The Reporting page's switchable content sections - same "local section switcher, no new navigation surface" shape every other multi-section module page uses (see <c>ViewModels.HR.HrSection</c>).</summary>
public enum ReportingSection
{
    Catalog,
    Viewer,
    Saved,
    Recent,
}
