namespace Rojan.Desktop.Presentation.ViewModels.Dashboard;

/// <summary>Display shape for one Recent Activity row - placeholder layout data, not a domain/business entity.</summary>
public sealed record ActivityItem(string Description, string Timestamp);
